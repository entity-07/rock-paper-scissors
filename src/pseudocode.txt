import random
import Enum
class Opponent: 
  class Choice(Enum):
    ROCK = 1
    PAPER = 2
    SCISSORS = 3

  def __init__(self, choice):
    self.choice = random.choice(list(Opponent.Choice))

class Player: 
  def __init__(self, choice)
  self.choice = input("Rock, Paper, or Scissors")


if Player.choice == Opponent.choice: 
  print("Its a draw")

if Player.choice == "Rock"
